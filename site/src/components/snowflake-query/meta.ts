import type { ComponentMeta } from "../../registry";

export const meta: ComponentMeta = {
  id: "snowflake-query",
  name: "snowflake-query",
  title: "Snowflake query",
  accent: "oklch(0.62 0.13 220)",
  summary: "Approve a Snowflake query before it runs. Warehouse + database + schema + role chips, scan-size hint, credit estimate.",
  loc: 200,
  status: "stable",
  category: "data",
};
